<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= typeof title !== 'undefined' ? title : 'My Todos' %></title>
    <style>
      :root {
        --bg: #0f172a;
        --card: #111827;
        --muted: #94a3b8;
        --text: #e5e7eb;
        --primary: #3b82f6;
        --primary-600: #2563eb;
        --danger: #ef4444;
        --success: #10b981;
        --border: #1f2937;
        --warning: #f59e0b;
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto,
          Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        background: radial-gradient(
            1200px 600px at 20% -10%,
            #172554 5%,
            transparent 60%
          ),
          radial-gradient(
            1200px 600px at 120% 110%,
            #1e293b 10%,
            transparent 60%
          ),
          var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      .container {
        width: 100%;
        max-width: 980px;
        margin: 0 auto;
        padding: 20px;
      }
      .nav {
        position: sticky;
        top: 0;
        z-index: 10;
        background: rgba(17, 24, 39, 0.75);
        backdrop-filter: blur(6px);
        border-bottom: 1px solid var(--border);
      }
      .nav .inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 20px;
      }
      .brand {
        font-weight: 700;
        letter-spacing: 0.3px;
      }
      .links {
        display: flex;
        gap: 14px;
        align-items: center;
      }
      .link {
        color: var(--text);
        text-decoration: none;
        padding: 8px 10px;
        border-radius: 8px;
      }
      .link.active {
        background: rgba(255, 255, 255, 0.06);
      }
      .logout {
        color: #fecaca;
      }
      .page {
        flex: 1;
      }
      .header {
        display: flex;
        align-items: end;
        justify-content: space-between;
        gap: 12px;
        margin: 22px 0 12px;
      }
      h1 {
        margin: 0;
        font-size: 26px;
      }
      .muted {
        color: var(--muted);
      }
      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 16px;
      }
      .card {
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.02),
          rgba(255, 255, 255, 0.01)
        );
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 16px;
      }
      .todo-title {
        font-weight: 700;
        margin: 0 0 8px;
        font-size: 16px;
      }
      .todo-content {
        color: var(--muted);
        font-size: 14px;
        margin: 0 0 10px;
      }
      .row {
        display: flex;
        gap: 10px;
        align-items: center;
        flex-wrap: wrap;
      }
      .btn {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 8px 10px;
        border-radius: 8px;
        border: 1px solid var(--border);
        background: rgba(255, 255, 255, 0.04);
        color: var(--text);
        text-decoration: none;
        cursor: pointer;
      }
      .btn.primary {
        background: var(--primary);
        border-color: var(--primary-600);
      }
      .btn.danger {
        background: rgba(239, 68, 68, 0.15);
        border-color: rgba(239, 68, 68, 0.45);
        color: #fecaca;
      }
      .btn.ghost {
        background: transparent;
      }
      .badge {
        display: inline-block;
        padding: 4px 8px;
        border-radius: 999px;
        font-size: 12px;
        border: 1px solid var(--border);
      }
      .done {
        color: #bbf7d0;
        border-color: rgba(16, 185, 129, 0.4);
        background: rgba(16, 185, 129, 0.08);
      }
      .not-done {
        color: #fecaca;
        border-color: rgba(239, 68, 68, 0.35);
        background: rgba(239, 68, 68, 0.08);
      }
      .published {
        color: #d1fae5;
        border-color: rgba(16, 185, 129, 0.4);
        background: rgba(16, 185, 129, 0.08);
      }
      .unpublished {
        color: #fde68a;
        border-color: rgba(245, 158, 11, 0.45);
        background: rgba(245, 158, 11, 0.08);
      }
      .field {
        display: grid;
        gap: 6px;
        margin-bottom: 12px;
      }
      label {
        font-size: 13px;
        color: var(--muted);
      }
      input[type="text"],
      textarea {
        width: 100%;
        padding: 10px 12px;
        border-radius: 8px;
        border: 1px solid var(--border);
        background: rgba(17, 24, 39, 0.6);
        color: var(--text);
      }
      textarea {
        min-height: 90px;
        resize: vertical;
      }
      .msg {
        padding: 10px 12px;
        border-radius: 10px;
        border: 1px solid var(--border);
        margin: 12px 0;
      }
      .msg.error {
        border-color: rgba(239, 68, 68, 0.45);
        background: rgba(239, 68, 68, 0.08);
        color: #fecaca;
      }
      .msg.success {
        border-color: rgba(16, 185, 129, 0.45);
        background: rgba(16, 185, 129, 0.08);
        color: #bbf7d0;
      }
      .split {
        display: grid;
        grid-template-columns: 1fr;
        gap: 16px;
      }
      @media (min-width: 860px) {
        .split {
          grid-template-columns: 1fr 1fr;
        }
      }
      .pagination {
        display: flex;
        justify-content: center;
        gap: 8px;
        margin: 20px 0;
      }
      .page-btn {
        padding: 8px 12px;
        border: 1px solid var(--border);
        border-radius: 8px;
        color: var(--text);
        text-decoration: none;
      }
      .page-btn[aria-disabled="true"] {
        opacity: 0.5;
        pointer-events: none;
      }
    </style>
  </head>
  <body>
    <nav class="nav">
      <div class="inner container">
        <div class="brand">Todo App</div>
        <div class="links">
          <a class="link <%= routeName === 'home' ? 'active' : '' %>" href="/"
            >Home</a
          >
          <a
            class="link <%= routeName === 'my' ? 'active' : '' %>"
            href="/my-todo"
            >My todo</a
          >
          <a class="link logout" href="/auth/logout">Logout</a>
        </div>
      </div>
    </nav>

    <main class="container page" role="main">
      <div class="header">
        <h1><%= typeof title !== 'undefined' ? title : 'My Todos' %></h1>
        <p class="muted">
          Manage your tasks<% if (user && user.name) { %>, <%= user.name %><% }
          %>.
        </p>
      </div>

      <% if (error) { %>
      <div class="msg error"><%= error %></div>
      <% } %> <% if (success) { %>
      <div class="msg success"><%= success %></div>
      <% } %>

      <section class="split">
        <!-- Create form -->
        <form class="card" method="POST" action="/my-todo">
          <h3 class="todo-title">Create new todo</h3>
          <div class="field">
            <label for="title">Title</label>
            <input
              id="title"
              name="title"
              type="text"
              placeholder="e.g. Buy groceries"
              required
            />
          </div>
          <div class="field">
            <label for="content">Content</label>
            <textarea
              id="content"
              name="content"
              placeholder="Details (optional)"
            ></textarea>
          </div>
          <div class="row" style="gap: 12px">
            <div class="field" style="flex: 1">
              <label for="from">From</label>
              <input id="from" name="from" type="datetime-local" required />
            </div>
            <div class="field" style="flex: 1">
              <label for="to">To</label>
              <input id="to" name="to" type="datetime-local" required />
            </div>
          </div>
          <div class="row" style="justify-content: space-between">
            <label style="display: flex; align-items: center; gap: 8px">
              <input type="checkbox" name="isPublish" value="on" /> Publish
            </label>
            <button class="btn primary" type="submit">Create</button>
          </div>
        </form>

        <!-- List & manage -->
        <div class="card">
          <h3 class="todo-title" style="margin-bottom: 10px">Your todos</h3>
          <% if (!todos || todos.length === 0) { %>
          <p class="muted">No todos yet. Create one on the left.</p>
          <% } else { %> <% const toLocal = (d) => { try { const dt = new
          Date(d); if (isNaN(dt)) return ''; const pad = (n)=>
          String(n).padStart(2,'0'); const y=dt.getFullYear(); const
          m=pad(dt.getMonth()+1); const da=pad(dt.getDate()); const
          h=pad(dt.getHours()); const mi=pad(dt.getMinutes()); return
          `${y}-${m}-${da}T${h}:${mi}`; } catch(e){ return ''; } }; const human
          = (d) => { try { const dt = new Date(d); if (isNaN(dt)) return '';
          return dt.toLocaleString(); } catch(e){ return ''; } }; %>
          <div class="grid">
            <% todos.forEach(t => { %>
            <article class="card">
              <form method="POST" action="/my-todo/<%= t.id %>/update">
                <div class="field">
                  <label>Title</label>
                  <input
                    type="text"
                    name="title"
                    value="<%= t.title %>"
                    required
                  />
                </div>
                <div class="field">
                  <label>Content</label>
                  <textarea name="content"><%= t.content || '' %></textarea>
                </div>
                <div class="row" style="gap: 12px">
                  <div class="field" style="flex: 1">
                    <label>From</label>
                    <input
                      type="datetime-local"
                      name="from"
                      value="<%= toLocal(t.from) %>"
                      required
                    />
                  </div>
                  <div class="field" style="flex: 1">
                    <label>To</label>
                    <input
                      type="datetime-local"
                      name="to"
                      value="<%= toLocal(t.to) %>"
                      required
                    />
                  </div>
                </div>
                <div
                  class="row"
                  style="justify-content: space-between; margin-bottom: 8px"
                >
                  <span class="badge <%= t.isDone ? 'done' : 'not-done' %>"
                    ><%= t.isDone ? 'Done' : 'Not done' %></span
                  >
                  <span
                    class="badge <%= t.isPublish ? 'published' : 'unpublished' %>"
                    ><%= t.isPublish ? 'Published' : 'Private' %></span
                  >
                </div>
                <p class="muted" style="font-size: 12px; margin: -4px 0 6px">
                  <span>From: <%= human(t.from) %></span>
                  <span style="margin: 0 6px">→</span>
                  <span>To: <%= human(t.to) %></span>
                </p>
                <div class="row" style="justify-content: space-between">
                  <label style="display: flex; align-items: center; gap: 8px">
                    <input type="checkbox" name="isPublish" value="on" <%=
                    t.isPublish ? 'checked' : '' %> /> Publish
                  </label>
                  <div class="row">
                    <button class="btn" type="submit">Save</button>
                    <button
                      class="btn ghost"
                      type="submit"
                      formmethod="post"
                      formaction="/my-todo/<%= t.id %>/toggle-done"
                      formnovalidate
                    >
                      <%= t.isDone ? 'Mark Undone' : 'Mark Done' %>
                    </button>
                    <button
                      class="btn ghost"
                      type="submit"
                      formmethod="post"
                      formaction="/my-todo/<%= t.id %>/toggle-publish"
                      formnovalidate
                    >
                      <%= t.isPublish ? 'Make Private' : 'Publish' %>
                    </button>
                    <button
                      class="btn danger"
                      type="submit"
                      formmethod="post"
                      formaction="/my-todo/<%= t.id %>/delete"
                      formnovalidate
                      onclick="return confirm('Delete this todo?');"
                    >
                      Delete
                    </button>
                  </div>
                </div>
              </form>
            </article>
            <% }) %>
          </div>
          <% } %> <% const totalPages = Math.max(Math.ceil((totalCount || 0) /
          (limit || 10)), 1); const current = Math.min(Math.max(page || 1, 1),
          totalPages); const q = new URLSearchParams(); q.set('limit',
          String(limit || 10)); %>
          <div class="pagination">
            <% q.set('page', String(current - 1)); %>
            <a
              class="page-btn"
              href="?<%= q.toString() %>"
              aria-disabled="<%= current <= 1 %>"
              >Prev</a
            >
            <span class="muted" style="padding: 8px 4px"
              >Page <%= current %> of <%= totalPages %></span
            >
            <% q.set('page', String(current + 1)); %>
            <a
              class="page-btn"
              href="?<%= q.toString() %>"
              aria-disabled="<%= current >= totalPages %>"
              >Next</a
            >
          </div>
        </div>
      </section>
    </main>
  </body>
</html>
