<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= typeof title !== 'undefined' ? title : 'Home' %></title>
    <style>
      :root {
        --bg: #0f172a;
        --card: #111827;
        --muted: #94a3b8;
        --text: #e5e7eb;
        --primary: #3b82f6;
        --primary-600: #2563eb;
        --danger: #ef4444;
        --success: #10b981;
        --border: #1f2937;
      }
      * {
        box-sizing: border-box;
      }
      body {
        margin: 0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto,
          Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        background: radial-gradient(
            1200px 600px at 20% -10%,
            #172554 5%,
            transparent 60%
          ),
          radial-gradient(
            1200px 600px at 120% 110%,
            #1e293b 10%,
            transparent 60%
          ),
          var(--bg);
        color: var(--text);
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }
      .container {
        width: 100%;
        max-width: 980px;
        margin: 0 auto;
        padding: 20px;
      }
      .nav {
        position: sticky;
        top: 0;
        z-index: 10;
        background: rgba(17, 24, 39, 0.75);
        backdrop-filter: blur(6px);
        border-bottom: 1px solid var(--border);
      }
      .nav .inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 12px 20px;
      }
      .brand {
        font-weight: 700;
        letter-spacing: 0.3px;
      }
      .links {
        display: flex;
        gap: 14px;
        align-items: center;
      }
      .link {
        color: var(--text);
        text-decoration: none;
        padding: 8px 10px;
        border-radius: 8px;
      }
      .link.active {
        background: rgba(255, 255, 255, 0.06);
      }
      .logout {
        color: #fecaca;
      }

      .page {
        flex: 1;
      }
      .header {
        display: flex;
        align-items: end;
        justify-content: space-between;
        gap: 12px;
        margin: 22px 0 12px;
      }
      h1 {
        margin: 0;
        font-size: 26px;
      }
      .muted {
        color: var(--muted);
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
        gap: 16px;
      }
      .card {
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.02),
          rgba(255, 255, 255, 0.01)
        );
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 16px;
        min-height: 120px;
      }
      .todo-title {
        font-weight: 700;
        margin: 0 0 8px;
        font-size: 16px;
      }
      .todo-content {
        color: var(--muted);
        font-size: 14px;
        margin: 0;
      }
      .badge {
        display: inline-block;
        margin-top: 10px;
        padding: 4px 8px;
        border-radius: 999px;
        font-size: 12px;
        border: 1px solid var(--border);
      }
      .done {
        color: #bbf7d0;
        border-color: rgba(16, 185, 129, 0.4);
        background: rgba(16, 185, 129, 0.08);
      }
      .not-done {
        color: #fecaca;
        border-color: rgba(239, 68, 68, 0.35);
        background: rgba(239, 68, 68, 0.08);
      }

      .pagination {
        display: flex;
        justify-content: center;
        gap: 8px;
        margin: 20px 0;
      }
      .page-btn {
        padding: 8px 12px;
        border: 1px solid var(--border);
        border-radius: 8px;
        color: var(--text);
        text-decoration: none;
      }
      .page-btn[aria-disabled="true"] {
        opacity: 0.5;
        pointer-events: none;
      }
    </style>
  </head>
  <body>
    <nav class="nav">
      <div class="inner container">
        <div class="brand">Todo App</div>
        <div class="links">
          <a class="link <%= routeName === 'home' ? 'active' : '' %>" href="/"
            >Home</a
          >
          <a
            class="link <%= routeName === 'my' ? 'active' : '' %>"
            href="/my-todo"
            >My todo</a
          >
          <a class="link logout" href="/auth/logout">Logout</a>
        </div>
      </div>
    </nav>

    <main class="container page" role="main">
      <div class="header">
        <h1><%= typeof title !== 'undefined' ? title : 'Home' %></h1>
        <p class="muted">
          Welcome<% if (user && user.name) { %>, <%= user.name %><% } %>.
        </p>
      </div>

      <% if (!todos || todos.length === 0) { %>
      <p class="muted">No todos to show.</p>
      <% } else { %> <% const human = (d) => { try { const dt = new Date(d); if
      (isNaN(dt)) return ''; return dt.toLocaleString(); } catch(e){ return '';
      } }; %>
      <section class="grid">
        <% todos.forEach(t => { %>
        <article class="card">
          <h3 class="todo-title"><%= t.title %></h3>
          <p class="todo-content">
            <%= (t.content && t.content.length > 160) ? (t.content.slice(0, 160)
            + '…') : (t.content || '') %>
          </p>
          <p class="muted" style="font-size: 12px; margin: 6px 0 0">
            <span>From: <%= human(t.from) %></span>
            <span style="margin: 0 6px">→</span>
            <span>To: <%= human(t.to) %></span>
          </p>
          <span class="badge <%= t.isDone ? 'done' : 'not-done' %>">
            <%= t.isDone ? 'Done' : 'Not done' %>
          </span>
        </article>
        <% }) %>
      </section>
      <% } %> <% const totalPages = Math.max(Math.ceil((totalCount || 0) /
      (limit || 10)), 1); const current = Math.min(Math.max(page || 1, 1),
      totalPages); const q = new URLSearchParams(); q.set('limit', String(limit
      || 10)); %>
      <div class="pagination">
        <% q.set('page', String(current - 1)); %>
        <a
          class="page-btn"
          href="?<%= q.toString() %>"
          aria-disabled="<%= current <= 1 %>"
          >Prev</a
        >
        <span class="muted" style="padding: 8px 4px"
          >Page <%= current %> of <%= totalPages %></span
        >
        <% q.set('page', String(current + 1)); %>
        <a
          class="page-btn"
          href="?<%= q.toString() %>"
          aria-disabled="<%= current >= totalPages %>"
          >Next</a
        >
      </div>
    </main>
  </body>
</html>
